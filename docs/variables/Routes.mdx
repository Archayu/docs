[**Discolink**](../README.md)

***

[Discolink](../globals.md) / Routes

# Variable: Routes

> `const` **Routes**: `object`

Defined in: [Constants/Routes.ts:1](https://github.com/Archayu/discolink/blob/ddfd70ea55cc6996bf3567765f64fb3824cd3f79/src/Constants/Routes.ts#L1)

## Type Declaration

### websocket()

> `readonly` **websocket**(): `"/websocket"`

#### Returns

`"/websocket"`

### trackLoading()

> `readonly` **trackLoading**(): `"/loadtracks"`

#### Returns

`"/loadtracks"`

### trackDecoding()

> `readonly` **trackDecoding**(`multiple?`): `"/decodetracks"` \| `"/decodetrack"`

#### Parameters

##### multiple?

`boolean`

#### Returns

`"/decodetracks"` \| `"/decodetrack"`

### player()

> `readonly` **player**(`sessionId`, `guildId?`): `` `/sessions/${string}/players/${string}` `` \| `` `/sessions/${string}/players` ``

#### Parameters

##### sessionId

`string`

##### guildId?

`string`

#### Returns

`` `/sessions/${string}/players/${string}` `` \| `` `/sessions/${string}/players` ``

### session()

> `readonly` **session**(`sessionId`): `` `/sessions/${string}` ``

#### Parameters

##### sessionId

`string`

#### Returns

`` `/sessions/${string}` ``

### info()

> `readonly` **info**(): `"/info"`

#### Returns

`"/info"`

### stats()

> `readonly` **stats**(): `"/stats"`

#### Returns

`"/stats"`

### routePlanner()

> `readonly` **routePlanner**(`free?`): `"/routeplanner/free/address"` \| `"/routeplanner/free/all"` \| `"/routeplanner/status"`

#### Parameters

##### free?

`"address"` | `"all"`

#### Returns

`"/routeplanner/free/address"` \| `"/routeplanner/free/all"` \| `"/routeplanner/status"`
